# 这个文件用于存放与外部API接口相关的配置，例如推荐系统API、数据抓取API等。

# 推荐系统API配置
RECOMMENDATION_API = {
    "url": "https://api.recommendation-system.com",
    "api_key": "your-api-key-here",  # 替换为实际API密钥
    "timeout": 30  # 请求超时时间
}

# 外部数据抓取API配置
EXTERNAL_API = {
    "url": "https://api.external-data.com",
    "api_key": "your-api-key-here",  # 替换为实际API密钥
    "timeout": 30  # 请求超时时间
}
